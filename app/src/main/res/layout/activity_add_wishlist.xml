<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:weightSum="1">

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter Item"
        android:id="@+id/my_item_input"
        android:layout_weight="0.30"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_marginTop="41dp" />

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="number"
        android:ems="10"
        android:id="@+id/my_price_input"
        android:hint="Enter Threshold Price"
        android:layout_weight="0.30"
        android:layout_marginBottom="57dp"
        android:layout_above="@+id/wishlist_submit_button"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Submit"
        android:id="@+id/wishlist_submit_button"
        android:layout_weight="0.30"
        android:layout_marginLeft="90dp"
        android:layout_marginStart="90dp"
        android:layout_centerVertical="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Cancel"
        android:id="@+id/wishlist_cancel_button"
        android:layout_gravity="right"
        android:layout_weight="0.30"
        android:layout_alignTop="@+id/wishlist_submit_button"
        android:layout_toRightOf="@+id/wishlist_submit_button"
        android:layout_toEndOf="@+id/wishlist_submit_button"
        android:layout_marginLeft="52dp"
        android:layout_marginStart="52dp" />

</RelativeLayout>